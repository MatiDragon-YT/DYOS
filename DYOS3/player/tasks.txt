Menu.SetColumn(hPanel, 0, 'M00008Y','M00001!',none,none,none,none,none,none,none,none,none,none,none)
Text.PrintHelp('M00001[') // PARA SCRIPTS / NO ESCENAS.
Menu_Take_Selection()

repeat
  if Menu_Close_Forced()
  then jump @Menu_Close
  end

	if Menu_Enter()
	then

		if 0A9A: FILE_HANDLE = DIR_DYOS "ab"
		then

	    SMX_add_actor_SPS()

			if _SELECTED == 0
			then
				FILE_COMMAND = VM_PLAYER_IS_KILL_TO_ACTOR
	    	call @SPS_Array_Actor_Get 2 SPS_INDEX_ACTORS B_ACTOR_ID result SMX_A
			end
			
			SMX_write_command()
			Fs.Close(FILE_HANDLE)
			Aplicated() 
		end
		Menu_Take_Selection()
	end
until Menu_Return()