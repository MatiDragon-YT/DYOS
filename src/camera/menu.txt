Menu.SetActiveItem($PANEL, 0)
:MenuDeLaCamara
if $ModoDeCompilacion == ePara_PC
then 0@s = 'M0000IU'
else 0@s = 'M0000PS'
end
Menu.SetColumn($PANEL, 0, 'M0000IR','M000002','M000003','M0000IS','M0000IT',0@s,'M0000JQ','M0000JR','M0000O7','M0000OA','M0000RR',{'M0000MM'}none,none)
FrenarRetorno()
FrenarSeleccion()
repeat
	wait 0
	if EntrarAlMenu()
	then
		if _Selected == 0 // Solid
		then
			if $ModoDeCompilacion == ePara_PC
			then get_cleo_shared_var 4@ = CAMARA_ESTATICA
			else Android.GetVar(4@, CAMARA_ESTATICA)
			end
			
			if 4@ == false
			then
				4@ = true
				Enable()
			else
				4@ = false
				Camera.RestoreJumpcut()
				Disable()
			end

			if $ModoDeCompilacion == ePar a_PC
			then set_cleo_shared_var CAMARA_ESTATICA = 4@
			else Android.SetVar(CAMARA_ESTATICA, 4@)
			end
		end

		if _Selected == 1 // Restablecer
		then
			if $ModoDeCompilacion == ePara_PC
			then
				set_cleo_shared_var CAMARA_CINEMATICA = false
				set_cleo_shared_var CAMARA_TIPO = 0
				set_cleo_shared_var CAMARA_TAMBALEO = 0
				set_cleo_shared_var CAMARA_AGITACION = 0.0
				set_cleo_shared_var CAMARA_HEBRIEDAD = 0
				set_cleo_shared_var CAMARA_PANORAMA = 70.0
				set_cleo_shared_var CAMARA_FILTRO = ENUM_FILTRO_NINGUNO
				set_cleo_shared_var CAMARA_ESTATICA = false
				set_cleo_shared_var CAMARA_MIRAR = ENUM_MIRAR_NADA
				set_cleo_shared_var CARARA_ANCLAR = ENUM_MIRAR_NADA
			else
				Android.SetVar(CAMARA_CINEMATICA, false)
				Android.SetVar(CAMARA_TIPO, 0)
				Android.SetVar(CAMARA_TAMBALEO, 0) 
				Android.SetVar(CAMARA_AGITACION, 0.0)
				Android.SetVar(CAMARA_HEBRIEDAD, 0) 
				Android.SetVar(CAMARA_PANORAMA, 70.0)
				Android.SetVar(CAMARA_FILTRO, ENUM_FILTRO_NINGUNO)
				Android.SetVar(CAMARA_ESTATICA, false)
				Android.SetVar(CAMARA_MIRAR, ENUM_MIRAR_NADA)
				Android.SetVar(CARARA_ANCLAR, ENUM_MIRAR_NADA)
			end
			
			Display.SetThermalVision(false)
			Display.SetNightVision(false)
			Display.SetWidescreen(false)
			
			if $ModoDeCompilacion == ePara_Android
			then
				0DD9: write_mem_addr 2571800 value 255.0 size 4 add_ib 1 protect 1 
				0DD9: write_mem_addr 2833896 value 255.0 size 4 add_ib 1 protect 1
			end

			Camera.RestoreJumpcut()
			Aplicated()
		end

		if _Selected == 2 // Nocturna
		then
			if $ModoDeCompilacion == ePara_PC
			then get_cleo_shared_var 4@ = CAMARA_FILTRO
			else Android.GetVar(4@, CAMARA_FILTRO)
			end
			if 4@ == ENUM_FILTRO_NOCTURNO
			then
				Display.SetThermalVision(false)
				Display.SetNightVision(false)
				4@ = ENUM_FILTRO_NINGUNO
				Disable()
			else
				Display.SetThermalVision(false)
				Display.SetNightVision(true)
				4@ = ENUM_FILTRO_NOCTURNO
				Enable()
			end

			if $ModoDeCompilacion == ePara_PC
			then set_cleo_shared_var CAMARA_FILTRO = 4@
			else Android.SetVar(CAMARA_FILTRO, 4@)
			end
		end

		if _Selected == 3 // Termica
		then
			if $ModoDeCompilacion == ePara_PC
			then get_cleo_shared_var 4@ = CAMARA_FILTRO
			else Android.GetVar(4@, CAMARA_FILTRO)
			end
			if 4@ == ENUM_FILTRO_TERMICO
			then
				Display.SetThermalVision(false)
				Display.SetNightVision(false)
				4@ = ENUM_FILTRO_NINGUNO
				Disable()
			else
				Display.SetThermalVision(true)
				Display.SetNightVision(false)
				4@ = ENUM_FILTRO_TERMICO
				Enable()
			end

			if $ModoDeCompilacion == ePara_PC
			then set_cleo_shared_var CAMARA_FILTRO = 4@
			else Android.SetVar(CAMARA_FILTRO, 4@)
			end
		end

		if _Selected == 4 // disepear - desaparecer hud
		then {$I ./disepear.txt}
		end

		if _Selected == 5 // stagger - tambaleo
		then {$I ./stagger.txt}
		end

		if _Selected == 6 // shake - agitacion
		then {$I ./shake.txt}
		end

		if _Selected == 7 // dizziness - mareo
		then {$I ./dizziness.txt}
		end

		if _Selected == 8 // fov - campo de vision
		then {$I ./fov.txt}
		end

		if _Selected == 9 // see - mirar
		then {$I ./see.txt}
		end

		if _Selected == 10 // attach jugador
		then
			if FirstVehicleNotDefined()
			then jump @MenuDeLaCamara
			end

			Vehicle_ShowSelector()
			FrenarSeleccion()
			10@ = -1
			Menu.SetActiveItem($PANEL,0)
			:MenuDeLaCamara_AnclarAlJugador
			Menu.SetColumn($PANEL,0,'M0000MM','M0000MB',none,none,none,none,none,none,none,none,none,none,none)
			FrenarRetorno()
			repeat
				Wait0()
				if ParaIzquierda()
				then
					10@ -= 1
					if 10@ < -1
					then 10@ = 9
					end
					FrenarIzquierda()
				end
				if ParaDerecha()
				then
					10@ += 1
					if 10@ > 9
					then 10@ = -1
					end
					FrenarDerecha()
				end
				if 10@ == 0
				then Menu.SetItemWithNumber($PANEL,0,0,'M0000RY',10@)
				else Menu.SetItemWithNumber($PANEL,0,0,'M0000MA',10@)
				end
				if 10@ == -1
				then Menu.SetItemWithNumber($PANEL,0,0,'M0000MB',10@)
				end

				if Selecionas()
				then
					if 10@ == -1
					then Camera.RestoreJumpcut()
					end
					if 10@ == 0
					then
						// custom
						float 11@ = 0.0 // x
						float 12@ = -9.0 // y
						float 13@ = 1.6 // z
						float 14@ = 0.0 // x2
						float 15@ = 0.0 // y2
						float 16@ = 0.0 // z2
						int 17@ = false // mod

						if $ModoDeCompilacion == ePara_PC
						then get_cleo_shared_var 18@ = CAMARA_PANORAMA
						else Android.GetVar(18@, CAMARA_PANORAMA)
						end
						EsconderMenu()
						repeat
							Wait0()
							if ParaEspecial()
							then
								if 17@ == true
								then 17@ = false
								else 17@ = true
								end
								FrenarEspecial()
							end
							if 17@ == true
							then
								if ParaDerecha()
								then 14@ += 0.025
								end
								if ParaIzquierda()
								then 14@ -= 0.025
								end
								if ParaAdelante()
								then 15@ += 0.025
								end
								if ParaAtras()
								then 15@ -= 0.025
								end 
								if ParaAceptar()
								then 16@ += 0.025
								end
								if ParaNegar()
								then 16@ -= 0.025
								end
							else
								if ParaDerecha()
								then
									11@ += 0.025
									14@ += 0.025
								end
								if ParaIzquierda()
								then
									11@ -= 0.025
									14@ -= 0.025
								end
								if ParaAdelante()
								then
									12@ += 0.025
									15@ += 0.025
								end
								if ParaAtras()
								then
									12@ -= 0.025
									15@ -= 0.025
								end 
								if ParaAceptar()
								then
									13@ += 0.025
									16@ += 0.025
								end
								if ParaNegar()
								then
									13@ -= 0.025
									16@ -= 0.025
								end
							end
							if ParaDisminuir()
							then
								18@ -= 0.5
								if 18@ < 0.5
								then 18@ = 0.5
								end
							end
							if ParaAumentar()
							then
								18@ += 0.5
								if 18@ > 130.0
								then 18@ = 130.0
								end
							end
							Camera.SetLerpFov(18@, 0.0, 200, 1)

							Txd.DrawRect(90.0, 59.0, 180.0, 90.0, 0x0,0x0,0x0,0xCC)
							Text.SetScale(0.48, 1.12)

							float 0@ = 11@
							3@s = 'M000011'
							5@ = 22.5
							PrintFloat()
							float 0@ = 12@
							3@s = 'M000012'
							5@ = 34.5
							PrintFloat()
							float 0@ = 13@
							3@s = 'M000013'
							5@ = 46.5
							PrintFloat()
							float 0@ = 14@
							3@s = 'M000018'
							5@ = 58.5
							PrintFloat()
							float 0@ = 15@
							3@s = 'M000019'
							5@ = 70.5
							PrintFloat()
							float 0@ = 16@
							3@s = 'M00001A'
							5@ = 82.5
							PrintFloat()

							Camera.AttachToCar($Auto($indAuto,101i), 11@, 12@, 13@, 14@, 15@, 16@, 0.0, SwitchType.JumpCut)

							if $ModoDeCompilacion == ePara_PC
							then set_cleo_shared_var CAMARA_PANORAMA = 18@
							else Android.SetVar(CAMARA_PANORAMA, 18@)
							end
						until ParaEscape()
						AparecerMenu()
						jump @MenuDeLaCamara_AnclarAlJugador
					end
					if Actor.IsDriving($PLAYER_ACTOR)
					then
						if Actor.IsDrivingCar($PLAYER_ACTOR)
						then
							if 10@ == 1
							then Camera.AttachToActor($PLAYER_ACTOR, 0.5, 0.3, 0.666, 90.0, -180.0, 0.0, 0.0, SwitchType.JumpCut)
							end
							if 10@ == 2
							then Camera.AttachToActor($PLAYER_ACTOR, 0.5, -1.1, 0.666, 0.0, 180.0, 0.0, 0.0, SwitchType.JumpCut)
							end
							if 10@ == 3
							then Camera.AttachToActor($PLAYER_ACTOR, 0.2, -0.1, 0.666, -270.0, 180.0, 0.0, 0.0, SwitchType.JumpCut)
							end
							if 10@ == 4
							then Camera.AttachToActor($PLAYER_ACTOR, 0.2, 0.1, 0.666, 270.0, 180.0, 0.0, 0.0, SwitchType.JumpCut)
							end
							if 10@ == 5
							then Camera.AttachToActor($PLAYER_ACTOR, 0.2, 0.0, 0.666, 0.0, -180.0, 0.0, 0.0, SwitchType.JumpCut)
							end
							if 10@ == 6
							then Camera.AttachToActor($PLAYER_ACTOR, 0.5, 0.2, 0.666, 0.0, 200.0, 0.0, 0.0, SwitchType.JumpCut)
							end
							if 10@ == 7
							then Camera.AttachToActor($PLAYER_ACTOR, 0.5, 0.3, 0.666, 180.0, 0.0, 0.0, 0.0, SwitchType.JumpCut)
							end
							if 10@ == 8
							then Camera.AttachToActor($PLAYER_ACTOR, 0.5, 0.3, 0.666, -180.0, 0.0, 0.0, 0.0, SwitchType.JumpCut)
							end
							if 10@ == 9
							then Camera.AttachToActor($PLAYER_ACTOR, 0.5, 0.3, 0.666, -90.0, -180.0, 0.0, 0.0, SwitchType.JumpCut)
							end
						end

						if Actor.IsDrivingBike($PLAYER_ACTOR)
						then
							if 10@ == 1
							then Camera.AttachToActor($PLAYER_ACTOR, 0.0, 0.51, 0.31, 0.0, 180.0, 0.0, 0.0, SwitchType.JumpCut)
							end
							if 10@ == 2
							then Camera.AttachToActor($PLAYER_ACTOR, 0.3, -0.3, 0.5555, 0.0, 180.0, 0.0, 0.0, SwitchType.JumpCut)
							end
							if 10@ == 3
							then Camera.AttachToActor($PLAYER_ACTOR, 0.0, 0.51, 0.31, 0.0, 180.0, 0.0, 0.0, SwitchType.JumpCut)
							end
							if 10@ == 4
							then Camera.AttachToActor($PLAYER_ACTOR, 0.5, 0.3, 0.666, 180.0, 0.0, 0.0, 0.0, SwitchType.JumpCut)
							end
							if 10@ == 5
							then Camera.AttachToActor($PLAYER_ACTOR, 0.5, 0.3, 0.666, -180.0, 0.0, 0.0, 0.0, SwitchType.JumpCut)
							end
							if 10@ == 6
							then Camera.AttachToActor($PLAYER_ACTOR, 0.5, 0.3, 0.666, -90.0, -180.0, 0.0, 0.0, SwitchType.JumpCut)
							end
							if 10@ == 7
							then Camera.AttachToActor($PLAYER_ACTOR, 0.5, 0.3, 0.666, 90.0, -180.0, 0.0, 0.0, SwitchType.JumpCut)
							end
							if 10@ == 8
							then Camera.AttachToActor($PLAYER_ACTOR, 0.2, -0.1, 0.666, -270.0, 180.0, 0.0, 0.0, SwitchType.JumpCut)
							end
							if 10@ == 9
							then Camera.AttachToActor($PLAYER_ACTOR, 0.2, 0.1, 0.666, 270.0, 180.0, 0.0, 0.0, SwitchType.JumpCut)
							end
						end
					end
				end
			until SalirDelMenu()
			Menu.SetActiveItem($PANEL,10)
			jump @MenuDeLaCamara
		end
		FrenarSeleccion()
	end
until SalirDelMenu()

Menu.SetActiveItem($PANEL, menu_de_la_camara)
jump @MenuPrincipal