:CrearObjeto 
    if $numObjeto < 101
    then
        $numObjeto++  
        
        Model.Load(#WOODENBOX)
        repeat
            wait 0
        until Model.Available(#WOODENBOX)
        
        $indObjeto++
        $Objeto($indObjeto,101i) = Object.Create(#WOODENBOX, $tempvar_X_coord, $tempvar_Y_coord, $tempvar_Z_coord)
        Object.Angle($Objeto($indObjeto,101i)) = $tempvar_Angle
        Object.SetLinkInInterior($Objeto($indObjeto,101i), $Active_Interior)
        Object.SetImmunities($Objeto($indObjeto,101i), true, true, true, true, true)
        Object.SetCollisionDetection($Objeto($indObjeto,101i), false)  
        
        Model.Release(#WOODENBOX) 
        $colObjeto($indObjeto,101i) = false 
        MensajeDeAplicado()     
    else
        Text.PrintHelp('M00000M')  
    end
return